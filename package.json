{
  "name": "greenhouse",
  "version": "0.0.0",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm run dev --workspace apps/docs",
    "dev:playground": "npm run dev --workspace apps/playground",
    "build": "npm run build --workspace @adeeeeta/greenhouse && npm run build --workspace apps/docs"
  }
}